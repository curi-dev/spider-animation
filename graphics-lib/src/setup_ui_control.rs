


pub struct SetupUiControl {
    pub is_active: bool
}

impl SetupUiControl {
    pub fn new() -> Self {

        Self {
            is_active: false
        }
    }

    // fn disable_control(&mut self) {
    //     self.is_active = false;
    // }

    // fn enable_control(&mut self) {
    //     self.is_active = true;
    // }
}